#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - an entry to print whether the number stored in the variable n is positive or negative.
 * Return:(0) Success
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	return (0);
}
