#include "main.h"
/**
 * mul - checks for checks for a digit (0 through 9).
 * @a: a - first in
 * @b: b - second int
 * Return: addition of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
